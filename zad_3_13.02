#include <stdio.h>
#include <stdlib.h>

int poramni_broj(int broj){
    int cifra, nov_broj=0;
    while(broj>0){
        cifra = broj%10;
        broj = broj / 10;
        if (cifra == 9){
            cifra = 7;
        }
        nov_broj = nov_broj*10 + cifra;
    }
    broj = nov_broj;
    nov_broj = 0;
    while(broj>0){
        cifra = broj%10;
        broj = broj / 10;
        nov_broj = nov_broj*10 + cifra;
    }
    return nov_broj;
}

int main()
{

    int n, counter=0, temp_n, cifra, nov_broj;
    int niza[100];

    while(scanf("%d", &n)){
        nov_broj = poramni_broj(n);
        niza[counter] = nov_broj;
        counter++;
        //printf("%d %d\n", n, nov_broj);
    }

    int i, k=counter-1, j, temp;
    for(i=0;i<counter-1;i++){
        for(j=0;j<counter-i;j++){
            if(niza[i] > niza[i+1]){
                temp=niza[i];
                niza[i] = niza[i+1];
                niza[i+1] = temp;
            }
        }
        //k--;
    }

    for(i=0;i<counter;i++){
        printf("%d ", niza[i]);
    }
}
